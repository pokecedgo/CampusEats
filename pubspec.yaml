name: campuseats
description: A Flutter project for Campus Eats application
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.12.1  # Updated to match firebase_auth requirements
  firebase_auth: ^5.5.1  # Compatible with firebase_core ^3.12.1
  cloud_firestore: ^3.1.0
  cloud_functions: ^4.0.6
  # Add other dependencies here

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  # Add assets here if needed
  assets:
    - assets/hogwartsDining.jpg
    - assets/Hogwartscrest.jpg
    - assets/ravenclaw.jpg